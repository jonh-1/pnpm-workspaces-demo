{
  "name": "livekit-pnpm-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --filter agent build",
    "agent:dev": "pnpm --filter agent dev",
    "agent:build": "pnpm --filter agent build",
    "agent:start": "pnpm --filter agent start",
    "agent:typecheck": "pnpm --filter agent typecheck",
    "agent:test": "pnpm --filter agent test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.2",
  "dependencies": {
    "@livekit/agents": "^1.3.0"
  }
}
